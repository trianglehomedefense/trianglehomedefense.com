# Triangle Home Defense Website Deployment

This repository is NOT the canonical GitHub Pages owner for `trianglehomedefense.com`.

Before any production website change, read the deployment source of truth in:

`trianglehomedefense/trianglehomedefense.github.io/DEPLOYMENT.md`

Current production owner: `trianglehomedefense/trianglehomedefense.github.io`
Production branch: `main`
Custom domain: `trianglehomedefense.com`

Do not add a `CNAME` here unless production ownership is intentionally moved and the canonical deployment protocol is updated in the same change.
